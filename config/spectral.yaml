# Pipeline Spectral config — extends the root .spectral.yaml
# Single source of truth for rules is .spectral.yaml
# This file exists because scripts/lint.py --ruleset points here.
extends:
  - ../.spectral.yaml