---
title: تحسينات فحص السلامة
description: بيانات وصفية إثرائية للمخططات المتعلقة بفحص السلامة
sidebar:
  order: 1
i18n:
  sourceHash: 8a6393a0c1e6
  translator: machine
---

بيانات وصفية إثرائية للمخططات المتعلقة بفحص السلامة. راجع [امتدادات OpenAPI](../development/#openapi-extensions) للاطلاع على تعريفات الامتدادات.

## المخططات المُثراة

### النمط

جميع المخططات المطابقة لـ `healthcheck.*SpecType` تتلقى إثراءات:

- `healthcheckCreateSpecType`
- `healthcheckReplaceSpecType`
- `healthcheckGetSpecType`

بالإضافة إلى ذلك، يحتوي المخطط المتداخل `healthcheckHttpHealthCheck` على إثراءات لتكوين فحص السلامة عبر HTTP.

## القيم الافتراضية المُطبَّقة من الخادم

الحقول المميزة بـ `x-f5xc-server-default: true` يتم تطبيق قيمتها الافتراضية (`default`) بواسطة خادم واجهة برمجة تطبيقات F5 XC عند حذفها من الطلبات.

### حقول المستوى الأعلى

| الحقل | القيمة الافتراضية | النوع | الوصف |
|-------|-------------------|-------|-------|
| `jitter` | `0` | عدد صحيح | قيمة الارتعاش المطلقة لعشوائية التوقيت |
| `jitter_percent` | `0` | عدد صحيح | الارتعاش المبني على النسبة المئوية لعشوائية التوقيت |

### مخطط healthcheckHttpHealthCheck

| الحقل | القيمة الافتراضية | النوع | الوصف |
|-------|-------------------|-------|-------|
| `use_origin_server_name` | `{}` | كائن | اسم خادم المصدر لترويسة Host |
| `headers` | `{}` | كائن | ترويسات مخصصة |
| `request_headers_to_remove` | `[]` | مصفوفة | الترويسات المراد إزالتها من الطلبات |
| `use_http2` | `false` | قيمة منطقية | دعم HTTP/2 |
| `expected_status_codes` | `[]` | مصفوفة | رموز الحالة المقبولة (فارغة = 200-299) |

## القيم الموصى بها

الحقول المميزة بـ `x-f5xc-recommended-value` تشير إلى القيم التي تملؤها وحدة تحكم الويب F5 XC مسبقاً عند إنشاء موارد جديدة.

### حقول المستوى الأعلى

| الحقل | القيمة الموصى بها | النوع | الوصف |
|-------|-------------------|-------|-------|
| `timeout` | `3` | عدد صحيح | مهلة فحص السلامة بالثواني |
| `interval` | `15` | عدد صحيح | الفاصل الزمني بين فحوصات السلامة بالثواني |
| `unhealthy_threshold` | `1` | عدد صحيح | الإخفاقات المتتالية قبل التعليم كغير سليم |
| `healthy_threshold` | `3` | عدد صحيح | النجاحات المتتالية قبل التعليم كسليم |
| `jitter_percent` | `30` | عدد صحيح | نسبة الارتعاش للاستخدام في بيئة الإنتاج |

### مخطط healthcheckHttpHealthCheck

| الحقل | القيمة الموصى بها | النوع | الوصف |
|-------|-------------------|-------|-------|
| `path` | `"/"` | سلسلة نصية | مسار نقطة نهاية فحص السلامة |
| `use_http2` | `false` | قيمة منطقية | إعداد دعم HTTP/2 |
| `expected_status_codes` | `["200"]` | مصفوفة | رموز الحالة التي تشير إلى سلامة المصدر |
| `use_origin_server_name` | `{}` | كائن | اسم خادم المصدر لترويسة Host |

## توصيات متغيرات OneOf

المخططات التي تحتوي على مجموعات حقول متنافية (OneOf) تتضمن `x-f5xc-recommended-oneof-variant` للإشارة إلى المتغير الأكثر استخداماً. تحدد هذه البيانات الوصفية الخيار النموذجي عند وجود خيارات متعددة، بناءً على إعدادات وحدة تحكم F5 XC الافتراضية وأنماط الاستخدام الشائعة.

### مجموعات OneOf للمستوى الأعلى

| المخطط | مجموعة OneOf | المتغير الموصى به | الوصف |
|--------|-------------|-------------------|-------|
| `healthcheckCreateSpecType` | `health_check` | `http_health_check` | نوع فحص السلامة عبر HTTP |
| `healthcheckReplaceSpecType` | `health_check` | `http_health_check` | نوع فحص السلامة عبر HTTP |

### مجموعات OneOf لمخطط healthcheckHttpHealthCheck

| المخطط | مجموعة OneOf | المتغير الموصى به | الوصف |
|--------|-------------|-------------------|-------|
| `healthcheckHttpHealthCheck` | `host_header_choice` | `use_origin_server_name` | ترويسة Host لطلبات فحص السلامة |

#### خيار ترويسة Host

مجموعة OneOf `host_header_choice` تتحكم في كيفية تحديد ترويسة Host في طلبات HTTP لفحص السلامة.

| المتغير | النوع | الوصف |
|---------|-------|-------|
| `use_origin_server_name` | `object` (فارغ) | استخدام اسم خادم المصدر كترويسة Host. هذا هو الخيار الافتراضي في وحدة تحكم F5 XC. |
| `host_header` | `string` | تحديد قيمة مخصصة لترويسة Host لطلبات فحص السلامة. |

**مرجع مخطط واجهة برمجة التطبيقات**: `x-ves-oneof-field-host_header_choice: ["host_header", "use_origin_server_name"]`

## مرجع امتدادات OpenAPI

تُضاف هذه الامتدادات الخاصة بالمورّد إلى مخطط OpenAPI القياسي لنقل سلوك F5 XC الافتراضي المحدد.

### x-f5xc-server-default

**النوع**: `boolean`

عندما تكون القيمة `true`، يشير ذلك إلى أن القيمة الافتراضية (`default`) المصاحبة يتم فرضها بواسطة خادم واجهة برمجة تطبيقات F5 XC. يمكن حذف الحقول التي تحمل هذا الامتداد بأمان من طلبات واجهة برمجة التطبيقات — حيث يطبق الخادم القيمة الافتراضية تلقائياً.

```yaml
use_http2:
  type: boolean
  default: false
  x-f5xc-server-default: true
```

### x-f5xc-recommended-value

**النوع**: `any` (يطابق نوع الحقل)

يحدد قيمة تستخدمها وحدة تحكم الويب F5 XC كقيمة افتراضية مملوءة مسبقاً. هذه القيمة ليست مفروضة من الخادم ولكنها تمثل التكوين الابتدائي النموذجي للموارد الجديدة المُنشأة عبر وحدة التحكم.

```yaml
timeout:
  type: integer
  x-f5xc-recommended-value: 3
```

### x-f5xc-recommended-oneof-variant

**النوع**: `object` (خريطة من اسم المجموعة إلى اسم المتغير)

للمخططات ذات مجموعات الحقول المتنافية، يحدد المتغير الذي يكون الخيار الافتراضي أو الأكثر شيوعاً. المفتاح هو اسم مجموعة OneOf والقيمة هي اسم حقل المتغير الموصى به.

```yaml
healthcheckCreateSpecType:
  type: object
  x-f5xc-recommended-oneof-variant:
    health_check: "http_health_check"
```
## الوصول إلى البيانات

### مواصفات OpenAPI

| الملف | المحتوى |
|-------|---------|
| `docs/specifications/api/virtual.json` | `healthcheckHttpHealthCheck`، `healthcheckCreateSpecType`، `healthcheckReplaceSpecType`، `healthcheckGetSpecType` |
| `docs/specifications/api/openapi.json` | المواصفات المدمجة مع جميع المخططات |

### هيكل validation.json

```text
defaults.resources.healthcheck
├── server_applied      # الحقول ذات x-f5xc-server-default: true
├── recommended         # الحقول ذات x-f5xc-recommended-value
├── oneof_recommended   # توصيات متغيرات OneOf
└── nested_recommended  # القيم الموصى بها للمخططات المتداخلة
```

## الوثائق ذات الصلة

- [دليل التطوير - امتدادات OpenAPI](../development/#openapi-extensions) - تعريفات الامتدادات واستخدامها
- [مواصفات التحقق](../validation-spec/) - تنسيق وهيكل validation.json
- [تحسينات مجمّع المصادر](./originpool-enhancements/) - إثراءات مخطط مجمّع المصادر

## سجل التغييرات

| الإصدار | التاريخ | التغييرات |
|---------|---------|-----------|
| 2.1.4 | 2026-01-19 | إضافة توثيق مجموعة OneOf `host_header_choice` لمعاملات طلبات فحص السلامة عبر HTTP |
| 2.1.3 | 2026-01-18 | دمج وثائق الامتدادات العامة في DEVELOPMENT.md؛ البيانات الخاصة بالمورد فقط |
| 2.1.2 | 2026-01-18 | أُعيدت كتابته كمرجع واجهة برمجة تطبيقات صرف؛ إزالة الأمثلة التابعة واللغة التوجيهية |
| 2.1.1 | 2026-01-18 | إضافة القيم الموصى بها المتداخلة، المتغيرات الموصى بها لـ OneOf، امتداد `x-f5xc-recommended-oneof-variant` |
| 2.1.0 | 2026-01-18 | إضافة هيكل القيم الافتراضية الموحد في validation.json |
| 2.0.30 | 2026-01-16 | إضافة القيم الافتراضية المتداخلة لمخططات `$ref` |
| 2.0.29 | 2026-01-17 | القيم الافتراضية الأولية لفحص السلامة |
