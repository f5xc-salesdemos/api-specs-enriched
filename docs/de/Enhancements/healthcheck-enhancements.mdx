---
title: Healthcheck-Erweiterungen
description: Anreicherungsmetadaten für Healthcheck-bezogene Schemas
sidebar:
  order: 1
i18n:
  sourceHash: 8a6393a0c1e6
  translator: machine
---

Anreicherungsmetadaten für Healthcheck-bezogene Schemas. Siehe [OpenAPI-Erweiterungen](../development/#openapi-extensions) für Erweiterungsdefinitionen.

## Angereicherte Schemas

### Muster

Alle Schemas, die `healthcheck.*SpecType` entsprechen, erhalten Anreicherungen:

- `healthcheckCreateSpecType`
- `healthcheckReplaceSpecType`
- `healthcheckGetSpecType`

Zusätzlich enthält das verschachtelte Schema `healthcheckHttpHealthCheck` Anreicherungen für die HTTP-Healthcheck-Konfiguration.

## Serverseitig angewendete Standardwerte

Felder, die mit `x-f5xc-server-default: true` gekennzeichnet sind, erhalten ihren `default`-Wert vom F5 XC API-Server, wenn sie in Anfragen weggelassen werden.

### Felder auf oberster Ebene

| Feld | Standardwert | Typ | Beschreibung |
|------|--------------|-----|--------------|
| `jitter` | `0` | integer | Absoluter Jitter-Wert für Timing-Randomisierung |
| `jitter_percent` | `0` | integer | Prozentbasierter Jitter für Timing-Randomisierung |

### healthcheckHttpHealthCheck-Schema

| Feld | Standardwert | Typ | Beschreibung |
|------|--------------|-----|--------------|
| `use_origin_server_name` | `{}` | object | Origin-Server-Name für den Host-Header |
| `headers` | `{}` | object | Benutzerdefinierte Header |
| `request_headers_to_remove` | `[]` | array | Header, die aus Anfragen entfernt werden |
| `use_http2` | `false` | boolean | HTTP/2-Unterstützung |
| `expected_status_codes` | `[]` | array | Akzeptierte Statuscodes (leer = 200-299) |

## Empfohlene Werte

Felder, die mit `x-f5xc-recommended-value` gekennzeichnet sind, geben Werte an, die die F5 XC-Webkonsole beim Erstellen neuer Ressourcen vorausfüllt.

### Felder auf oberster Ebene

| Feld | Empfohlener Wert | Typ | Beschreibung |
|------|-------------------|-----|--------------|
| `timeout` | `3` | integer | Healthcheck-Timeout in Sekunden |
| `interval` | `15` | integer | Intervall zwischen Healthchecks in Sekunden |
| `unhealthy_threshold` | `1` | integer | Aufeinanderfolgende Fehler, bevor als ungesund markiert wird |
| `healthy_threshold` | `3` | integer | Aufeinanderfolgende Erfolge, bevor als gesund markiert wird |
| `jitter_percent` | `30` | integer | Jitter-Prozentsatz für den Produktionseinsatz |

### healthcheckHttpHealthCheck-Schema

| Feld | Empfohlener Wert | Typ | Beschreibung |
|------|-------------------|-----|--------------|
| `path` | `"/"` | string | Healthcheck-Endpunkt-Pfad |
| `use_http2` | `false` | boolean | HTTP/2-Unterstützungseinstellung |
| `expected_status_codes` | `["200"]` | array | Statuscodes, die einen gesunden Origin anzeigen |
| `use_origin_server_name` | `{}` | object | Origin-Server-Name für den Host-Header |

## OneOf-Varianten-Empfehlungen

Schemas, die sich gegenseitig ausschließende Feldgruppen (OneOf) enthalten, beinhalten `x-f5xc-recommended-oneof-variant`, um anzugeben, welche Variante am häufigsten verwendet wird. Diese Metadaten identifizieren die typische Wahl, wenn mehrere Optionen existieren, basierend auf F5 XC-Konsolenstandards und gängigen Nutzungsmustern.

### OneOf-Gruppen auf oberster Ebene

| Schema | OneOf-Gruppe | Empfohlene Variante | Beschreibung |
|--------|--------------|---------------------|--------------|
| `healthcheckCreateSpecType` | `health_check` | `http_health_check` | HTTP-Healthcheck-Typ |
| `healthcheckReplaceSpecType` | `health_check` | `http_health_check` | HTTP-Healthcheck-Typ |

### healthcheckHttpHealthCheck OneOf-Gruppen

| Schema | OneOf-Gruppe | Empfohlene Variante | Beschreibung |
|--------|--------------|---------------------|--------------|
| `healthcheckHttpHealthCheck` | `host_header_choice` | `use_origin_server_name` | Host-Header für Healthcheck-Anfragen |

#### Host-Header-Auswahl

Die `host_header_choice` OneOf-Gruppe steuert, wie der Host-Header in HTTP-Healthcheck-Anfragen angegeben wird.

| Variante | Typ | Beschreibung |
|----------|-----|--------------|
| `use_origin_server_name` | `object` (leer) | Verwendet den Origin-Server-Namen als Host-Header. Dies ist die Standardauswahl in der F5 XC-Konsole. |
| `host_header` | `string` | Gibt einen benutzerdefinierten Host-Header-Wert für Healthcheck-Anfragen an. |

**API-Schema-Referenz**: `x-ves-oneof-field-host_header_choice: ["host_header", "use_origin_server_name"]`

## OpenAPI-Erweiterungsreferenz

Diese herstellerspezifischen Erweiterungen werden dem Standard-OpenAPI-Schema hinzugefügt, um F5 XC-spezifisches Standardverhalten zu vermitteln.

### x-f5xc-server-default

**Typ**: `boolean`

Wenn `true`, zeigt dies an, dass der begleitende `default`-Wert vom F5 XC API-Server durchgesetzt wird. Felder mit dieser Erweiterung können sicher in API-Anfragen weggelassen werden – der Server wendet den Standardwert automatisch an.

```yaml
use_http2:
  type: boolean
  default: false
  x-f5xc-server-default: true
```

### x-f5xc-recommended-value

**Typ**: `any` (entspricht dem Feldtyp)

Gibt einen Wert an, den die F5 XC-Webkonsole als vorausgefüllten Standard verwendet. Dieser Wert wird nicht serverseitig durchgesetzt, sondern stellt die typische Ausgangskonfiguration für neue Ressourcen dar, die über die Konsole erstellt werden.

```yaml
timeout:
  type: integer
  x-f5xc-recommended-value: 3
```

### x-f5xc-recommended-oneof-variant

**Typ**: `object` (Zuordnung von Gruppenname zu Variantenname)

Für Schemas mit sich gegenseitig ausschließenden Feldgruppen wird angegeben, welche Variante die Standard- oder häufigste Wahl ist. Der Schlüssel ist der OneOf-Gruppenname und der Wert ist der empfohlene Varianten-Feldname.

```yaml
healthcheckCreateSpecType:
  type: object
  x-f5xc-recommended-oneof-variant:
    health_check: "http_health_check"
```
## Datenzugriff

### OpenAPI-Spezifikationen

| Datei | Inhalt |
|-------|--------|
| `docs/specifications/api/virtual.json` | `healthcheckHttpHealthCheck`, `healthcheckCreateSpecType`, `healthcheckReplaceSpecType`, `healthcheckGetSpecType` |
| `docs/specifications/api/openapi.json` | Zusammengeführte Spezifikation mit allen Schemas |

### validation.json-Struktur

```text
defaults.resources.healthcheck
├── server_applied      # Felder mit x-f5xc-server-default: true
├── recommended         # Felder mit x-f5xc-recommended-value
├── oneof_recommended   # OneOf-Varianten-Empfehlungen
└── nested_recommended  # Empfohlene Werte verschachtelter Schemas
```

## Verwandte Dokumentation

- [Entwicklungshandbuch - OpenAPI-Erweiterungen](../development/#openapi-extensions) – Erweiterungsdefinitionen und Verwendung
- [Validierungsspezifikation](../validation-spec/) – validation.json-Format und -Struktur
- [Origin-Pool-Erweiterungen](./originpool-enhancements/) – Origin-Pool-Schema-Anreicherungen

## Änderungsprotokoll

| Version | Datum | Änderungen |
|---------|-------|------------|
| 2.1.4 | 2026-01-19 | Dokumentation der `host_header_choice` OneOf-Gruppe für HTTP-Healthcheck-Anfrageparameter hinzugefügt |
| 2.1.3 | 2026-01-18 | Globale Erweiterungsdokumentation in DEVELOPMENT.md konsolidiert; nur ressourcenspezifische Daten |
| 2.1.2 | 2026-01-18 | Als reine API-Referenz umgeschrieben; nachgelagerte Beispiele und präskriptive Sprache entfernt |
| 2.1.1 | 2026-01-18 | Verschachtelte empfohlene Werte, OneOf-empfohlene Varianten, `x-f5xc-recommended-oneof-variant`-Erweiterung hinzugefügt |
| 2.1.0 | 2026-01-18 | Einheitliche Standardwerte-Struktur in validation.json hinzugefügt |
| 2.0.30 | 2026-01-16 | Verschachtelte Standardwerte für `$ref`-Schemas hinzugefügt |
| 2.0.29 | 2026-01-17 | Initiale Healthcheck-Standardwerte |
