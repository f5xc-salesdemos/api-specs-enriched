---
title: Miglioramenti Healthcheck
description: Metadati di arricchimento per gli schemi relativi agli healthcheck
sidebar:
  order: 1
i18n:
  sourceHash: 8a6393a0c1e6
  translator: machine
---

Metadati di arricchimento per gli schemi relativi agli healthcheck. Consultare [Estensioni OpenAPI](../development/#openapi-extensions) per le definizioni delle estensioni.

## Schemi arricchiti

### Pattern

Tutti gli schemi corrispondenti a `healthcheck.*SpecType` ricevono arricchimenti:

- `healthcheckCreateSpecType`
- `healthcheckReplaceSpecType`
- `healthcheckGetSpecType`

Inoltre, lo schema annidato `healthcheckHttpHealthCheck` contiene arricchimenti per la configurazione degli health check HTTP.

## Valori predefiniti applicati dal server

I campi contrassegnati con `x-f5xc-server-default: true` hanno il loro valore `default` applicato dal server API F5 XC quando omessi dalle richieste.

### Campi di primo livello

| Campo | Valore predefinito | Tipo | Descrizione |
|-------|-------------------|------|-------------|
| `jitter` | `0` | integer | Valore assoluto di jitter per la randomizzazione dei tempi |
| `jitter_percent` | `0` | integer | Jitter basato su percentuale per la randomizzazione dei tempi |

### Schema healthcheckHttpHealthCheck

| Campo | Valore predefinito | Tipo | Descrizione |
|-------|-------------------|------|-------------|
| `use_origin_server_name` | `{}` | object | Nome del server di origine per l'header Host |
| `headers` | `{}` | object | Header personalizzati |
| `request_headers_to_remove` | `[]` | array | Header da rimuovere dalle richieste |
| `use_http2` | `false` | boolean | Supporto HTTP/2 |
| `expected_status_codes` | `[]` | array | Codici di stato accettati (vuoto = 200-299) |

## Valori raccomandati

I campi contrassegnati con `x-f5xc-recommended-value` indicano valori che la console web F5 XC pre-popola durante la creazione di nuove risorse.

### Campi di primo livello

| Campo | Valore raccomandato | Tipo | Descrizione |
|-------|---------------------|------|-------------|
| `timeout` | `3` | integer | Timeout dell'health check in secondi |
| `interval` | `15` | integer | Intervallo tra gli health check in secondi |
| `unhealthy_threshold` | `1` | integer | Fallimenti consecutivi prima di contrassegnare come non sano |
| `healthy_threshold` | `3` | integer | Successi consecutivi prima di contrassegnare come sano |
| `jitter_percent` | `30` | integer | Percentuale di jitter per l'uso in produzione |

### Schema healthcheckHttpHealthCheck

| Campo | Valore raccomandato | Tipo | Descrizione |
|-------|---------------------|------|-------------|
| `path` | `"/"` | string | Percorso dell'endpoint dell'health check |
| `use_http2` | `false` | boolean | Impostazione del supporto HTTP/2 |
| `expected_status_codes` | `["200"]` | array | Codici di stato che indicano un'origine sana |
| `use_origin_server_name` | `{}` | object | Nome del server di origine per l'header Host |

## Raccomandazioni per le varianti OneOf

Gli schemi contenenti gruppi di campi mutuamente esclusivi (OneOf) includono `x-f5xc-recommended-oneof-variant` per indicare quale variante è più comunemente utilizzata. Questi metadati identificano la scelta tipica quando esistono più opzioni, basandosi sui valori predefiniti della console F5 XC e sui pattern di utilizzo comuni.

### Gruppi OneOf di primo livello

| Schema | Gruppo OneOf | Variante raccomandata | Descrizione |
|--------|-------------|----------------------|-------------|
| `healthcheckCreateSpecType` | `health_check` | `http_health_check` | Tipo health check HTTP |
| `healthcheckReplaceSpecType` | `health_check` | `http_health_check` | Tipo health check HTTP |

### Gruppi OneOf di healthcheckHttpHealthCheck

| Schema | Gruppo OneOf | Variante raccomandata | Descrizione |
|--------|-------------|----------------------|-------------|
| `healthcheckHttpHealthCheck` | `host_header_choice` | `use_origin_server_name` | Header Host per le richieste di health check |

#### Scelta dell'header Host

Il gruppo OneOf `host_header_choice` controlla come viene specificato l'header Host nelle richieste HTTP di health check.

| Variante | Tipo | Descrizione |
|----------|------|-------------|
| `use_origin_server_name` | `object` (vuoto) | Utilizza il nome del server di origine come header Host. Questa è la selezione predefinita nella console F5 XC. |
| `host_header` | `string` | Specifica un valore personalizzato per l'header Host nelle richieste di health check. |

**Riferimento schema API**: `x-ves-oneof-field-host_header_choice: ["host_header", "use_origin_server_name"]`

## Riferimento estensioni OpenAPI

Queste estensioni vendor vengono aggiunte allo schema OpenAPI standard per comunicare il comportamento predefinito specifico di F5 XC.

### x-f5xc-server-default

**Tipo**: `boolean`

Quando `true`, indica che il valore `default` associato viene applicato dal server API F5 XC. I campi con questa estensione possono essere tranquillamente omessi dalle richieste API: il server applica automaticamente il valore predefinito.

```yaml
use_http2:
  type: boolean
  default: false
  x-f5xc-server-default: true
```

### x-f5xc-recommended-value

**Tipo**: `any` (corrisponde al tipo del campo)

Specifica un valore che la console web F5 XC utilizza come valore predefinito pre-popolato. Questo valore non viene applicato dal server ma rappresenta la configurazione iniziale tipica per le nuove risorse create tramite la console.

```yaml
timeout:
  type: integer
  x-f5xc-recommended-value: 3
```

### x-f5xc-recommended-oneof-variant

**Tipo**: `object` (mappa dal nome del gruppo al nome della variante)

Per gli schemi con gruppi di campi mutuamente esclusivi, identifica quale variante è la scelta predefinita o più comune. La chiave è il nome del gruppo OneOf e il valore è il nome del campo della variante raccomandata.

```yaml
healthcheckCreateSpecType:
  type: object
  x-f5xc-recommended-oneof-variant:
    health_check: "http_health_check"
```
## Accesso ai dati

### Specifiche OpenAPI

| File | Contenuto |
|------|-----------|
| `docs/specifications/api/virtual.json` | `healthcheckHttpHealthCheck`, `healthcheckCreateSpecType`, `healthcheckReplaceSpecType`, `healthcheckGetSpecType` |
| `docs/specifications/api/openapi.json` | Specifica unificata con tutti gli schemi |

### Struttura di validation.json

```text
defaults.resources.healthcheck
├── server_applied      # Campi con x-f5xc-server-default: true
├── recommended         # Campi con x-f5xc-recommended-value
├── oneof_recommended   # Raccomandazioni per le varianti OneOf
└── nested_recommended  # Valori raccomandati per schemi annidati
```

## Documentazione correlata

- [Guida allo sviluppo - Estensioni OpenAPI](../development/#openapi-extensions) - Definizioni ed utilizzo delle estensioni
- [Specifica di validazione](../validation-spec/) - Formato e struttura di validation.json
- [Miglioramenti Origin Pool](./originpool-enhancements/) - Arricchimenti degli schemi origin pool

## Registro delle modifiche

| Versione | Data | Modifiche |
|----------|------|-----------|
| 2.1.4 | 2026-01-19 | Aggiunta documentazione del gruppo OneOf `host_header_choice` per i parametri delle richieste HTTP di health check |
| 2.1.3 | 2026-01-18 | Documentazione globale delle estensioni consolidata in DEVELOPMENT.md; solo dati specifici per risorsa |
| 2.1.2 | 2026-01-18 | Riscritto come puro riferimento API; rimossi esempi downstream e linguaggio prescrittivo |
| 2.1.1 | 2026-01-18 | Aggiunti valori raccomandati annidati, varianti OneOf raccomandate, estensione `x-f5xc-recommended-oneof-variant` |
| 2.1.0 | 2026-01-18 | Aggiunta struttura unificata dei valori predefiniti in validation.json |
| 2.0.30 | 2026-01-16 | Aggiunti valori predefiniti annidati per schemi `$ref` |
| 2.0.29 | 2026-01-17 | Valori predefiniti healthcheck iniziali |
