---
title: "\U0001F511 Authentication API"
description: การกำหนดค่าผู้ให้บริการ Identity และนโยบายการเข้าถึง
sidebar:
  hidden: true
i18n:
  sourceHash: 1cc771c65d3a
  translator: machine
---

การจัดการ Identity พร้อมการผสานรวมผู้ให้บริการ นโยบายการเข้าถึง และการควบคุมวงจรชีวิตของข้อมูลประจำตัว

- **หมวดหมู่**: แพลตฟอร์ม
- **ความซับซ้อน**: ง่าย
- **เส้นทาง**: 14 | **สคีมา**: 38
- **ระดับ**: Standard
- **โดเมนที่เกี่ยวข้อง**: `system`, `users`

## กรณีการใช้งาน

- กำหนดค่ากลไกการยืนยันตัวตน
- จัดการผู้ให้บริการ OIDC และ OAuth
- กำหนดค่าการจัดเตรียมผู้ใช้ SCIM
- จัดการข้อมูลประจำตัว API และการเข้าถึง
- กำหนดค่านโยบายการสมัครบัญชี

## ทรัพยากรหลัก

- **authentication_policy**: นโยบายการยืนยันตัวตนสำหรับผู้ใช้และการเข้าถึง API
- **token**: โทเค็น API สำหรับการเข้าถึงแบบโปรแกรม
- **api_credential**: ข้อมูลประจำตัว API สำหรับการยืนยันตัวตนของบริการ

## Endpoints

| วิธีการ | เส้นทาง | คำอธิบาย |
|--------|------|-------------|
| POST | `/api/web/namespaces/system/bulk_revoke/api_credentials` | เพิกถอน API credentials จำนวนมาก |
| POST | `/api/web/namespaces/system/bulk_revoke/service_credentials` | เพิกถอน service credential จำนวนมาก |
| POST | `/api/web/namespaces/&#123;namespace&#125;/activate/api_credentials` | เปิดใช้งาน API credential |
| POST | `/api/web/namespaces/&#123;namespace&#125;/activate/service_credentials` | เปิดใช้งาน API service credential |
| GET | `/api/web/namespaces/&#123;namespace&#125;/api_credentials` | แสดงรายการ API Credentials |
| POST | `/api/web/namespaces/&#123;namespace&#125;/api_credentials` | สร้าง API Credentials |
| GET | `/api/web/namespaces/&#123;namespace&#125;/api_credentials/&#123;name&#125;` | ดึงข้อมูล API Credentials |
| POST | `/api/web/namespaces/&#123;namespace&#125;/renew/api_credentials` | ต่ออายุ API credential |
| POST | `/api/web/namespaces/&#123;namespace&#125;/renew/service_credentials` | ต่ออายุ API service credential |
| POST | `/api/web/namespaces/&#123;namespace&#125;/revoke/api_credentials` | เพิกถอน API credential |
| POST | `/api/web/namespaces/&#123;namespace&#125;/revoke/scim_token` | เพิกถอน SCIM API credential |
| POST | `/api/web/namespaces/&#123;namespace&#125;/revoke/service_credentials` | เพิกถอน Service credential |
| GET | `/api/web/namespaces/&#123;namespace&#125;/scim_token` | ดึงข้อมูล Scim Token |
| POST | `/api/web/namespaces/&#123;namespace&#125;/scim_token` | สร้าง/สร้างใหม่ SCIM API token |
| GET | `/api/web/namespaces/&#123;namespace&#125;/service_credentials` | แสดงรายการ service credentials |
| POST | `/api/web/namespaces/&#123;namespace&#125;/service_credentials` | สร้าง service credentials |
| GET | `/api/web/namespaces/&#123;namespace&#125;/service_credentials/&#123;name&#125;` | ดึงข้อมูล Service Credential |
| PUT | `/api/web/namespaces/&#123;namespace&#125;/service_credentials/&#123;name&#125;` | แทนที่ service credentials |

## ลิงก์

- [เอกสารอ้างอิง API แบบโต้ตอบ](./authentication/)
- [OpenAPI Specification JSON](../specifications/api/authentication.json)
