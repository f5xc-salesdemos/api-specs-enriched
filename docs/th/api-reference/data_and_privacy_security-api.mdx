---
title: "\U0001F510 API ความปลอดภัยของข้อมูลและความเป็นส่วนตัว"
description: การตรวจจับ PII ประเภทข้อมูล และการปฏิบัติตามกฎระเบียบระดับภูมิภาค
sidebar:
  hidden: true
i18n:
  sourceHash: 64667b549cff
  translator: machine
---

นโยบายข้อมูลที่ละเอียดอ่อนพร้อมกฎการจำแนกประเภทแบบกำหนดเอง การกำหนดค่าภูมิภาค LMA และการควบคุมการปฏิบัติตามกฎระเบียบตามพื้นที่ทางภูมิศาสตร์

- **หมวดหมู่**: ความปลอดภัย
- **ความซับซ้อน**: ง่าย
- **เส้นทาง**: 11 | **สคีมา**: 65
- **ระดับ**: ขั้นสูง
- **โดเมนที่เกี่ยวข้อง**: `blindfold`, `client_side_defense`

## กรณีการใช้งาน

- กำหนดค่านโยบายการตรวจจับข้อมูลที่ละเอียดอ่อน
- กำหนดประเภทข้อมูลแบบกำหนดเองสำหรับการจำแนกประเภท PII
- จัดการการกำหนดค่าภูมิภาค LMA
- ผสานรวมการกำหนดค่าทางภูมิศาสตร์เพื่อการปฏิบัติตามกฎระเบียบ

## ทรัพยากรหลัก

- **sensitive_data_policy**: นโยบายข้อมูลที่ละเอียดอ่อนสำหรับการปกป้อง PII
- **data_classification**: การจำแนกประเภทข้อมูลสำหรับการจัดหมวดหมู่เนื้อหา

## ปลายทาง

| เมธอด | เส้นทาง | คำอธิบาย |
|--------|------|-------------|
| POST | `/api/config/namespaces/&#123;metadata.namespace&#125;/data_types` | สร้างประเภทข้อมูล |
| PUT | `/api/config/namespaces/&#123;metadata.namespace&#125;/data_types/&#123;metadata.name&#125;` | แทนที่ประเภทข้อมูล |
| GET | `/api/config/namespaces/&#123;namespace&#125;/data_types` | แสดงรายการประเภทข้อมูล |
| GET | `/api/config/namespaces/&#123;namespace&#125;/data_types/&#123;name&#125;` | ดึงประเภทข้อมูล |
| DELETE | `/api/config/namespaces/&#123;namespace&#125;/data_types/&#123;name&#125;` | ลบประเภทข้อมูล |
| GET | `/api/config/namespaces/&#123;namespace&#125;/geo_configs/&#123;name&#125;` | ดึงการกำหนดค่าทางภูมิศาสตร์ |
| GET | `/api/config/namespaces/&#123;namespace&#125;/lma_regions` | แสดงรายการภูมิภาค LMA |
| GET | `/api/config/namespaces/&#123;namespace&#125;/lma_regions/&#123;name&#125;` | ดึงภูมิภาค LMA |
| POST | `/api/config/namespaces/&#123;metadata.namespace&#125;/sensitive_data_policys` | สร้างการค้นพบข้อมูลที่ละเอียดอ่อน |
| PUT | `/api/config/namespaces/&#123;metadata.namespace&#125;/sensitive_data_policys/&#123;metadata.name&#125;` | แทนที่การค้นพบข้อมูลที่ละเอียดอ่อน |
| GET | `/api/config/namespaces/&#123;namespace&#125;/sensitive_data_policys` | แสดงรายการการค้นพบข้อมูลที่ละเอียดอ่อน |
| GET | `/api/config/namespaces/&#123;namespace&#125;/sensitive_data_policys/&#123;name&#125;` | ดึงการค้นพบข้อมูลที่ละเอียดอ่อน |
| DELETE | `/api/config/namespaces/&#123;namespace&#125;/sensitive_data_policys/&#123;name&#125;` | ลบการค้นพบข้อมูลที่ละเอียดอ่อน |

## ลิงก์

- [เอกสารอ้างอิง API แบบโต้ตอบ](./data_and_privacy_security/)
- [ไฟล์ข้อกำหนด OpenAPI JSON](../specifications/api/data_and_privacy_security.json)
