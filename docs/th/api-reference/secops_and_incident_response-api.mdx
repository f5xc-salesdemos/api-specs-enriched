---
title: "\U0001F6A8 API ปฏิบัติการรักษาความปลอดภัยและการตอบสนองต่อเหตุการณ์"
description: การตรวจจับภัยคุกคาม การให้คะแนนความเสี่ยงของผู้ใช้ และการบล็อกอัตโนมัติ
sidebar:
  hidden: true
i18n:
  sourceHash: e45eced4c860
  translator: machine
---

การบรรเทาผลกระทบจากผู้ใช้ที่เป็นอันตรายด้วยการจำแนกระดับภัยคุกคาม การดำเนินการตอบสนองอัตโนมัติสำหรับรูปแบบพฤติกรรมที่น่าสงสัย

- **หมวดหมู่**: ความปลอดภัย
- **ความซับซ้อน**: ง่าย
- **เส้นทาง**: 4 | **สคีมา**: 32
- **ระดับ**: ขั้นสูง
- **โดเมนที่เกี่ยวข้อง**: `bot_defense`, `virtual`, `network_security`

## กรณีการใช้งาน

- กำหนดค่านโยบายการบรรเทาภัยคุกคามอัตโนมัติ
- กำหนดกฎสำหรับการตรวจจับผู้ใช้ที่เป็นอันตราย
- จัดการเวิร์กโฟลว์การตอบสนองต่อเหตุการณ์
- ใช้การบล็อกหรือการจำกัดอัตราต่อภัยคุกคาม

## ทรัพยากรหลัก

- **mitigation_policy**: นโยบายการบรรเทาสำหรับการตอบสนองต่อเหตุการณ์
- **malicious_user_rule**: กฎผู้ใช้ที่เป็นอันตรายสำหรับการบรรเทาภัยคุกคาม

## จุดเชื่อมต่อ

| เมธอด | เส้นทาง | คำอธิบาย |
|--------|------|-------------|
| POST | `/api/config/namespaces/&#123;metadata.namespace&#125;/malicious_user_mitigations` | สร้างการบรรเทาผู้ใช้ที่เป็นอันตราย |
| PUT | `/api/config/namespaces/&#123;metadata.namespace&#125;/malicious_user_mitigations/&#123;metadata.name&#125;` | แทนที่การบรรเทาผู้ใช้ที่เป็นอันตราย |
| GET | `/api/config/namespaces/&#123;namespace&#125;/malicious_user_mitigations` | แสดงรายการการบรรเทาผู้ใช้ที่เป็นอันตราย |
| GET | `/api/config/namespaces/&#123;namespace&#125;/malicious_user_mitigations/&#123;name&#125;` | ดึงข้อมูลการบรรเทาผู้ใช้ที่เป็นอันตราย |
| DELETE | `/api/config/namespaces/&#123;namespace&#125;/malicious_user_mitigations/&#123;name&#125;` | ลบการบรรเทาผู้ใช้ที่เป็นอันตราย |

## ลิงก์

- [เอกสารอ้างอิง API แบบโต้ตอบ](./secops_and_incident_response/)
- [ข้อกำหนด OpenAPI JSON](../specifications/api/secops_and_incident_response.json)
